import { OnInit, EventEmitter, ElementRef, Renderer2, AfterContentInit, TemplateRef, OnDestroy, SimpleChanges } from '@angular/core';
import { ResizeService } from '../../directives/resize/index';
import { BehaviorSubject } from 'rxjs/BehaviorSubject';
import { Observable } from 'rxjs/Observable';
export declare class VirtualScrollComponent implements OnInit, AfterContentInit, OnDestroy {
    private _elementRef;
    collection: Observable<any[]>;
    cellHeight: number;
    loadOnScroll: boolean;
    loading: EventEmitter<number>;
    cellTemplate: TemplateRef<any>;
    loadingIndicatorTemplate: TemplateRef<any>;
    loadButtonTemplate: TemplateRef<any>;
    cells: BehaviorSubject<any[]>;
    scrollTop: number;
    isLoading: boolean;
    pageNumber: number;
    data: any[];
    loadingComplete: boolean;
    private _subscription;
    private _height;
    constructor(_elementRef: ElementRef, resizeService: ResizeService, renderer: Renderer2);
    ngOnInit(): void;
    ngAfterContentInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    ngOnDestroy(): void;
    setupObservable(): void;
    renderCells(): void;
    getVisibleCells(): any[];
    getTotalHeight(): number;
    loadNextPage(): void;
    reset(): void;
}
